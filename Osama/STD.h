#ifndef STD_H_
#define STD_H_

/*
 - we create the header file to give the data type

 - The guard file at the top used to protect the header file
 */

typedef unsigned char uint8;
typedef char int8;
typedef unsigned short uint16;
typedef short int16;
typedef unsigned int uint32;
typedef int int32;
typedef enum { false, true } bool; /*  used to give (1) as the student_ID is existed and gives (0) as the
                                      student_ID is not existed on the database  */


#endif
